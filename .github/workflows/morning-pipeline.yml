name: Morning Pipeline

permissions:
  actions: write
  contents: read

on:
  schedule:
    # 04:30 AM IST = 23:00 UTC
    # We trigger 2.5 hours early to bypass GitHub Actions' chronic scheduling delays.
    # The python script will deliberately sleep and wake up exactly at 07:00 AM IST.
    - cron: '0 23 * * *'
  workflow_dispatch:

jobs:
  pipeline:
    name: Scrape → Score → Generate → Send Brief
    runs-on: ubuntu-latest
    timeout-minutes: 180

    env:
      STATE_BACKEND: gist
      GIST_TOKEN: ${{ secrets.GIST_TOKEN }}
      GIST_ID: ${{ secrets.GIST_ID }}
      GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
      MISTRAL_API_KEY: ${{ secrets.MISTRAL_API_KEY }}
      NVIDIA_NIM_API_KEY: ${{ secrets.NVIDIA_NIM_API_KEY }}
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      LINKEDIN_ACCESS_TOKEN: ${{ secrets.LINKEDIN_ACCESS_TOKEN }}
      LINKEDIN_PERSON_URN: ${{ secrets.LINKEDIN_PERSON_URN }}
      REDDIT_CLIENT_ID: ${{ secrets.REDDIT_CLIENT_ID }}
      REDDIT_CLIENT_SECRET: ${{ secrets.REDDIT_CLIENT_SECRET }}
      REDDIT_USER_AGENT: linkedin-autopilot/1.0
      POST_TIME: "07:00"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install Playwright Chromium
        run: python -m playwright install chromium --with-deps

      - name: Run morning pipeline
        run: python scripts/run_pipeline.py
